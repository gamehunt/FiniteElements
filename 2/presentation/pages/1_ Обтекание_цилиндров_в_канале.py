import streamlit as st

menu = st.sidebar.radio(
    "***",
    (
        "Геометрическая модель",
        "Математическая модель",
        "Задачи исследования",
    ),
)


if menu == "Геометрическая модель":
    r"""
##### Геометрическая модель
**Расчетная область**
    """

    c1, c2 = st.columns([3, 1])
    c1.image("figs/5.png")

    r"""
    + $D_1, D_2$ — сечения первого и второго цилиндров
    + $r_1, r_2$ — радиусы цилиндров
    + $(l_1, h_1)$ и $(l_2, h_2)$ — координаты центров цилиндров в системе $(x_1, x_2)$
    + $l_2$ — координата центра второго цилиндра по $x_1$
    + $h_2$ — координата центра второго цилиндра по $x_2$
    + $\Omega$ — расчетная область
    + $L, H$ — длина и ширина расчетной части канала
    + $\Gamma_1, \Gamma_2$ — нижняя и верхняя стенки канала
    + $\Gamma_3, \Gamma_4$ — входная и выходная границы канала
    + $\gamma_1, \gamma_2$ — границы первого и второго цилиндров
    """

if menu == "Математическая модель":
    r"""
    ##### Математическая модель

    **Уравнение для функции тока**

    Рассматривается стационарное плоское потенциальное течение несжимаемой жидкости в области $\Omega$.

    $\begin{aligned}
    {-} \nabla^2 \psi = 0,
    \quad \bm x \in \Omega .
    \end{aligned}$

    **Краевые условия**

    На входе задается равномерный поток со скоростью $\bm u = \{1, 0\}$:

    $\begin{aligned}
    \psi(\bm x) = x_2,
    \quad \bm x \in \Gamma_3 .
    \end{aligned}$

    На выходе используется условие плоскопараллельного течения:

    $\begin{aligned}
    \frac{\partial \psi}{\partial n}(\bm x) = 0,
    \quad \bm x \in \Gamma_4 .
    \end{aligned}$

    На твердых стенках канала задается условие непротекания $\psi = \operatorname{const}$:

    $\begin{aligned}
    \psi(\bm x) = 0,
    \quad \bm x \in \Gamma_1, \qquad
    \psi(\bm x) = H,
    \quad \bm x \in \Gamma_2 .
    \end{aligned}$

    На границах цилиндров функция тока постоянна, а циркуляции принимаются нулевыми:

    $\begin{aligned}
    \psi(\bm x) = c_\alpha = \operatorname{const},
    \quad \bm x \in \gamma_\alpha, \qquad
    \oint_{\gamma_\alpha} \frac{\partial \psi}{\partial n}(\bm x) \, ds = 0,
    \quad \alpha = 1,2 .
    \end{aligned}$
    """

if menu == "Задачи исследования":
    r"""
    ##### Задачи исследования

    + Разработка вычислительного алгоритма на основе конечно-элементной аппроксимации по пространству.
    + Основной расчетный вариант:
        - канал: $L = 2$, $H = 1$;
        - первый цилиндр: $l_1 = 1/2$, $h_1 = 1/2$, $r_1 = 3/16$;
        - второй цилиндр расположен с координатами центра $l_2 = 5/4$ по $x_1$ и $h_2 = 1/2$ по $x_2$, его радиус $r_2 = 3/16$;
        - циркуляции на границах цилиндров $\gamma_1$ и $\gamma_2$ равны нулю.
    + Построение расчетных сеток с использованием генератора Gmsh:
        - три последовательно сгущающиеся сетки для базового варианта;
        - подробные сетки для вариантов с изменением $l_2$ и $h_2$.
    + Программная реализация конечно-элементного решения.
    + Верификация численных результатов на последовательности сеток по функции тока на границах цилиндров.
    + Исследование симметричного обтекания при степенях аппроксимации $p = 1,2,3$ и различных значениях $l_2$.
    + Исследование несимметричного обтекания при различных значениях координаты $h_2$.
    """
